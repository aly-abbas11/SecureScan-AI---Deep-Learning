name: CI

on:
  workflow_dispatch:

jobs:
  repository-check:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Repository status
        run: |
          echo "SecureScan AI repository workflow is set to manual mode."
          echo "Full model training and testing should be run locally or in notebook environment."
          echo "This avoids failed CI caused by missing datasets, model files, or import paths."
